<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>404 • ASCEND Robotics</title>
    <meta name="description" content="Page not found — ASCEND Robotics" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet" />

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              magenta: "#D5006D",
              orange:  "#FF9800",
              amber:   "#FFB300",
              purple:  "#673AB7",
              lavender:"#E1BEE7",
              dark:    "#0b0b12",
            },
            fontFamily: { inter: ["Inter", "system-ui", "sans-serif"] },
            borderRadius: { "2xl": "1.25rem" }
          }
        }
      }
    </script>

    <style>
      body{
        background:
          radial-gradient(1200px 600px at 10% -10%, #673AB722, transparent 60%),
          radial-gradient(1000px 500px at 110% 10%, #D5006D22, transparent 60%),
          linear-gradient(180deg, #0b0b12, #141427);
      }

      :root{
        --card: #151627; --card-outline: rgba(255,255,255,.08);
        --glow-1: rgba(255,255,255,.06); --glow-2: rgba(255,255,255,.35);
      }
      .hp-cards:hover > .hp-card::after { opacity: 1; }
      .hp-card{ position:relative; border-radius:16px; height:160px; }
      .hp-card::before,.hp-card::after{ content:""; position:absolute; inset:0; border-radius:inherit; opacity:0; transition:opacity 420ms; }
      .hp-card::before{ background: radial-gradient(600px circle at var(--mx) var(--my), var(--glow-1), transparent 40%); z-index:2; }
      .hp-card::after{  background: radial-gradient(520px circle at var(--mx) var(--my), var(--glow-2), transparent 40%); z-index:0; }
      .hp-card > .hp-content{ position:absolute; inset:1px; z-index:1; border-radius:inherit; background:var(--card); border:1px solid var(--card-outline); padding:18px; display:flex; gap:10px; align-items:center; }
      .hp-icon{ width:40px; height:40px; border-radius:10px; display:flex; align-items:center; justify-content:center; background:rgba(255,255,255,.06); }
      .hp-title{ font-weight:700; }
      .hp-sub{ font-size:.9rem; color:rgba(255,255,255,.7); }

      @keyframes blink { 0%, 92%, 100% { transform: scaleY(1); } 95% { transform: scaleY(.18); } }
      @keyframes eyeSwapIn  { 0%,30% { opacity:0; transform: scale(.8) rotate(-6deg); } 40%,55% { opacity:1; transform: scale(1) rotate(0deg);} 70%,100%{opacity:0;} }
      @keyframes eyeSwapOut { 0%,30% { opacity:1; } 40%,55% { opacity:0; } 70%,100%{opacity:1;} }
      @keyframes nosePulse { 0%,100% { transform: scale(1); filter: drop-shadow(0 0 0 rgba(255,179,0,0)); }
                             50%      { transform: scale(1.08); filter: drop-shadow(0 0 10px rgba(255,179,0,.55)); } }

      .eye-circle{ transform-origin: center; animation: eyeSwapOut 5.5s infinite; }
      .eye-four  { transform-origin: center; animation: eyeSwapIn 5.5s infinite; fill: #E1BEE7; font-weight: 800; }
      .nose-zero { transform-origin: center; animation: nosePulse 2.6s ease-in-out infinite; }

      footer.fixed-footer{
        position: fixed; left: 0; right: 0; bottom: 0;
        backdrop-filter: blur(10px);
        background: rgba(0,0,0,.45);
        border-top: 1px solid rgba(255,255,255,.12);
        z-index: 40;
      }
    </style>
  </head>

  <body class="font-inter text-white min-h-screen">
    <header class="sticky top-0 z-50 border-b border-white/10 bg-black/30 backdrop-blur-xl">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-3 flex items-center justify-between">
        <a href="/" class="flex items-center gap-3">
          <img
                src="/img/ascend-crest.png"
                alt="ASCEND crest"
                class="h-9 w-9 rounded-2xl object-cover ring-1 ring-white/10 shadow-sm"
                width="36" height="36" loading="eager" decoding="async" fetchpriority="high"
            />
          <span class="text-lg font-semibold tracking-wide">ASCEND Robotics</span>
        </a>
        <a href="/" class="rounded-xl bg-magenta px-4 py-2 text-sm font-medium shadow-lg hover:scale-[1.02]">Back home</a>
      </div>
    </header>

    <main>
      <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-28 text-center">
        <h1 class="text-[68px] sm:text-[96px] md:text-[120px] font-extrabold leading-none">
          <span class="bg-gradient-to-r from-magenta via-amber to-purple bg-clip-text text-transparent">404</span>
        </h1>
        <p class="mt-3 text-white/80 text-lg">That route doesn’t exist.</p>

        <div class="mx-auto mt-8 max-w-xl rounded-2xl border border-white/15 bg-white/10 p-6 shadow-xl backdrop-blur-xl">
          <svg viewBox="0 0 560 260" class="mx-auto w-full h-auto">
            <rect x="70" y="60" width="420" height="140" rx="28" ry="28" fill="#1b1b2b" stroke="#673AB7" stroke-width="3"/>
            <line x1="280" y1="60" x2="280" y2="34" stroke="#FFB300" stroke-width="4"/>
            <circle cx="280" cy="28" r="7" fill="#D5006D"/>
            <circle cx="200" cy="120" r="34" fill="#0f0f1a" stroke="#D5006D" stroke-width="3"/>
            <circle cx="360" cy="120" r="34" fill="#0f0f1a" stroke="#D5006D" stroke-width="3"/>

            <circle class="eye-circle" cx="200" cy="120" r="10" fill="#E1BEE7"/>
            <circle class="eye-circle" cx="360" cy="120" r="10" fill="#E1BEE7"/>

            <text class="eye-four" x="200" y="126" text-anchor="middle" font-size="36">4</text>
            <text class="eye-four" x="360" y="126" text-anchor="middle" font-size="36">4</text>

            <text class="nose-zero" x="280" y="152" text-anchor="middle" font-size="42" fill="#FFB300" font-weight="800">0</text>

            <rect x="248" y="172" width="64" height="12" rx="6" ry="6" fill="#FFB300"/>
            <rect x="150" y="200" width="70" height="12" rx="6" ry="6" fill="#151529"/>
            <rect x="340" y="200" width="70" height="12" rx="6" ry="6" fill="#151529"/>
          </svg>
          <p class="mt-2 text-sm text-white/70">Our scout bot is trying to read the map…</p>
        </div>

        <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3 hp-cards" id="linkCards">
          <a href="/#projects" class="hp-card">
            <div class="hp-content">
              <div class="hp-icon" style="background:#FFB30022; color:#FFB300;">
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M12 2v4m0 12v4m8-8h-4M8 12H4m10.5-5.5L17 7m-10 10l2.5 2.5" stroke-width="2"/></svg>
              </div>
              <div class="text-left">
                <div class="hp-title">Projects</div>
                <div class="hp-sub">See what we’re building.</div>
              </div>
            </div>
          </a>
          <a href="/#team" class="hp-card">
            <div class="hp-content">
              <div class="hp-icon" style="background:#D5006D22; color:#D5006D;">
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M16 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z" stroke-width="2"/><path d="M6 21a6 6 0 0 1 12 0" stroke-width="2"/></svg>
              </div>
              <div class="text-left">
                <div class="hp-title">Team</div>
                <div class="hp-sub">Meet the people behind the bots.</div>
              </div>
            </div>
          </a>
          <a href="/#contact" class="hp-card">
            <div class="hp-content">
              <div class="hp-icon" style="background:#673AB722; color:#673AB7;">
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor"><rect x="3" y="5" width="18" height="14" rx="2" stroke-width="2"/><path d="M4 7l8 6 8-6" stroke-width="2"/></svg>
              </div>
              <div class="text-left">
                <div class="hp-title">Contact</div>
                <div class="hp-sub">Reach out — we reply fast.</div>
              </div>
            </div>
          </a>
        </div>

        <div class="mt-8">
          <a href="/" class="rounded-xl bg-amber px-5 py-3 font-semibold text-black shadow-lg">Go to homepage</a>
        </div>
      </section>
    </main>

    <footer class="fixed-footer py-4 text-sm text-white/80">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 flex items-center justify-between">
        <div>© <script>document.write(new Date().getFullYear())</script> ASCEND Robotics</div>
        <div class="flex items-center gap-4">
          <a href="/" class="hover:text-white">Home</a>
          <a href="/#projects" class="hover:text-white">Projects</a>
          <a href="/#contact" class="hover:text-white">Contact</a>
        </div>
      </div>
    </footer>

    <script>
      const lc = document.getElementById("linkCards");
      if (lc) lc.onmousemove = (e)=>{
        for(const card of lc.getElementsByClassName("hp-card")){
          const r = card.getBoundingClientRect();
          card.style.setProperty("--mx", `${e.clientX - r.left}px`);
          card.style.setProperty("--my", `${e.clientY - r.top}px`);
        }
      };
    </script>
  </body>
</html>
